# Copy this file to ".env" and fill in your own key.
# If you leave GROQ_API_KEY empty or don't create a .env file at all,
# the app will still run fine using its built-in rule-based fallback
# recommendation instead of calling the Groq LLM.

GROQ_API_KEY=your_groq_api_key_here
